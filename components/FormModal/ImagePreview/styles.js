import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    imageContainer: {
        width: '100%',
        height: 220,
        padding: 10,
    },
    image: {
        width: '100%',
        height: '100%',
    },
});

export default styles;
